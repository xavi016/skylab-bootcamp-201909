function slice (array) {

    if (!(array instanceof Array)) throw TypeError (array + ' is not an array');

    var start = arguments.length > 1 ? arguments[2] : 0;
    var end = arguments.length > 2 ? arguments[3] : arguments[0].length;

    var newArray = [];

    
}