extends ../view/index

include ../feedback/index
include ../results/index

block body
    section.view.search
        h1.search__title Search
        h2.search__user= name
        a.search__favs(href=favsPath) Favorites
        form.search__logout(method="post" action=logout)
            button Logout
        form.search__form(method="get" action=path)
            span.search__icon 🐣
            input.search__criteria(type="text" name="q" placeholder="criteria" value=query || '')
            button.search__submit 🔍
        if error
            +feedback(error)
        if results
            +results(results, favPath, detailPath)